server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    username: postgres
    password: 123456
    url: jdbc:postgresql://localhost:5432/siso
  jpa:
    hibernate:
      ddl-auto: update